# Direct dependencies, pinned to the versions verified in production.
#
# Transitive dependencies are intentionally left to pip's resolver — pinning
# these thirteen is what stops an unrelated rebuild from silently pulling a new
# major Flask or cryptography.
#
# To see what a running container actually resolved:
#   docker exec everfly-app pip freeze
#
# To bump: raise a pin, rebuild, run `python -m unittest discover -s tests`,
# then deploy behind a new tag so rollback is one command away.

# Web framework
Flask==3.1.3
Werkzeug==3.1.8
Flask-Limiter==3.11.0
Flask-WTF==1.2.2
gunicorn==23.0.0

# Database
PyMySQL==1.2.0

# Crypto — encrypts users' stored FlightAware API keys.
# Read the changelog before bumping across a major version.
cryptography==49.0.0

# Configuration
python-dotenv==1.2.1

# HTTP client (FlightAware AeroAPI)
requests==2.32.5

# Reference data and time handling.
# airportsdata is a dataset release; bumping it changes airport lookups.
airportsdata==20250909
pycountry==24.6.1
python-dateutil==2.9.0.post0
pytz==2026.2
